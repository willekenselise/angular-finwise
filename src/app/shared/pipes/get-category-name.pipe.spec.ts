import { GetCategoryNamePipe } from './get-category-name.pipe';

describe('GetCategoryNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetCategoryNamePipe();
    expect(pipe).toBeTruthy();
  });
});
